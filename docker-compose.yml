database:
    image: mysql:5.7
    ports:
        - "3007:3006"
    volumes:
        - ./server/init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
        MYSQL_ROOT_PASSWORD: secret 
        MYSQL_DATABASE: dev
